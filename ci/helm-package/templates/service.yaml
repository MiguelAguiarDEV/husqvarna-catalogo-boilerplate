apiVersion: v1
kind: Service
metadata:
  name: {{ include "husqvarna-catalogo-2025.fullname" . }}
  labels:
    {{- include "husqvarna-catalogo-2025.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "husqvarna-catalogo-2025.selectorLabels" . | nindent 4 }}
