# Icons

This directory contains SVG icons that are used by the app.

The names of the icons for its use are in the dir: src/components/ui/icons

Everything in both directories is generated by `npm run build:icons`.
